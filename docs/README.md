# Docs

Long-form notes that don't belong in the top-level README:

* `benchmark.md` — extended results: per-class confusion, latency
  histograms, MAC budget derivation. Generated alongside `make benchmark`.
* `sweep.md` — accuracy vs. parameter count vs. MACs across width
  multipliers. Generated by `python -m scripts.sweep_sizes`.
* `accuracy_vs_macs.png` — plot from the size sweep.

Populated as Phases 4–5 land.
