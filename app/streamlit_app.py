"""Streamlit live mic demo (Stretch #1).

Speak into the laptop mic; see the live log-mel spectrogram and a
per-class probability bar chart updating in real time. Uses the bundled
INT8 ONNX model from ``assets/`` so a fresh clone can run the demo with
``make app`` without any training.

Implemented in Stretch Phase 6.
"""

from __future__ import annotations


def main() -> None:
    raise NotImplementedError("Stretch Phase 6: implement Streamlit demo.")


if __name__ == "__main__":
    main()
