# Notebooks

One-off exploratory notebooks. Not part of the pipeline; not required to
reproduce results. Strip outputs before committing
(`jupyter nbconvert --clear-output --inplace`).

Planned:

* `01_features_eda.ipynb` — class balance, per-class spectrogram montages,
  silence/unknown construction sanity checks.
